#include "main.h"

/**
 * mul -  checks for a digit
 *@a:   the     number  to      be      multiplied
 *@b:	the	number	to	be	multiplied
 * Return: Always a	*	b
 */
int mul(int a, int b)
{
	return	(a	*	b);
}
